const CharityModel = require('../user/user.model').CharityModel;
const ChartiyHelper = require('..')

function addCharity(data, callback){
    if(data){
        CharityModel.
    
    }


}

module.exports = { addCharity }